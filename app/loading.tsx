import React from "react";

// Minimal Suspense fallback — the actual loading overlay with fade-out
// animation is handled by template.tsx to avoid conflicts with page transitions.
export default function Loading() {
  return <></>;
}
